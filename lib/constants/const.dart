import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFF7900);
const secondaryColor = Color(0xFF000000);
const lightColor = Color(0xFFFFFFFF);
const textColor = Color(0xFF595959);
const placeholderColor = Color(0xFFD9D9D9);
const borderColor = Color(0xFFe4e4e4);

const pagePadding = EdgeInsets.all(40);
const elPadding = 8.0;
const imageSize = 277.0;

Widget addSpace(double height) {
  return SizedBox(
    height: height,
  );
}
